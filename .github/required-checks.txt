# Contexts required on `main` (and every protected branch a PR can target).
# One name per line. Comments and blanks ignored.
# The live list is GitHub settings; this file is what the repo can disagree with (#715).
fmt · clippy · test
footprint (RAM budget)
build release
scaled mode (parity · control plane · reconcile)
the compose fleet comes up
cargo-deny (vulns · licences · bans)
point-read latency
per-cursor RAM budget (dense multi-nest)
fuzz smoke (decode path)
Jules approval
