# RFC-0042 slice 6 closing experiments

Commit measured: `ae26610f7f2df4f0f7eadc46e070777daa197944`. Agent: `20260830T194831Z-994c939b`. Model: GPT-5.6. Host: Debian GNU/Linux 13, Linux 6.12.94+deb13-amd64, x86_64, Intel Core i9-13980HX (32 logical CPUs), 62 GiB RAM, glibc 2.41. Toolchain: rustc 1.95.0 (59807616e 2026-04-14).

## What I read.

`CLAUDE.md`; RFCs 0042, 0042 slice 0, 0041, 0034 and 0013; RFC 0043 section 5 only; `docs/benchmarks.md`; `docs/bench/noise-floor.md`; `tools/df-gate/`; the #986 raw record and its harness. The specified slice-5 decision input is absent from this checkout. I did not read any other slice-6 report.

## Pre-registration.

* A1 required 15 post-warm-up repeats at 1, 8 and 32 clients, A/B alternation, actual 4/8/16-connection pools, the hot+cold nest and CI-method peak RSS. At least 0.7 times Rust qps with p99 below twice Rust removes the row; about 40 qps after pooling is the contrary result; over 256 MB is a resource regression.
* A2 gives weight only to named users with concrete C++-closure blockers. Zero names means zero demonstrated user weight.
* A3 succeeds only with a release-workflow artefact, static libstdc++/libgcc, only libc/libm dependencies, and runs on the oldest README host plus one older.
* A4 required all admitted queries, all 12 stated authored views and a representative `/sql` log. At most about twelve fully covered shapes supports a layer; an uncovered tail establishes an engine.
* B3, B2 and B1 run only after Group A leaves removal standing. B3 over 256 MB is hard unless B1 fixes it inside a two-day box; B2 requires byte-exact parity; B1 requires the existing corpus per role.

## What I ran.

The branch was created at the stated commit. A `cp -a` copy of `/home/pepe/corpus/horizon-nest` lives at `/tmp/slice6-20260830T194831Z-994c939b/horizon-nest`: 375,591,323 bytes and 10,480 parquet segments. It contains 7 view files, no discoverable `sealed_through` value and no `/sql` request log. It cannot demonstrate the required hot+cold seam or satisfy A4's inputs.

`nuthatch doctor --dir <copy> --rpc <redacted Alchemy endpoint>` succeeded. The redacted raw log says archive depth is available, batches of at least 200 are fine, and the range-only probe recommends a 320-block window. Source: `raw/doctor-alchemy-redacted.log`.

**A1: waived, environment.** The available nest cannot cross the required seam. The #986 fixture is synthetic, uses 2 million rows and is not an admissible substitute. This checkout also retains `SQL_MAX_CONCURRENCY = 2`, while the requested experiment is 32 clients. No figure is reported.

**A2: completed.** The repository and GitHub issue search found the ABI fact and issue #946, but no named user, support thread or grant deliverable where someone is concretely blocked by `GLIBCXX_3.4.29`, musl, cross-compilation or a from-source build. #946 is authored by `cargopete` and states every README-listed platform clears the requirement. Result: zero named beneficiaries in inspected evidence. Source: `raw/a2-github-search.json`; this is a timestamp-prefixed log, not standalone JSON.

**A3: failed.** A local, non-release-workflow build with `-static-libstdc++ -static-libgcc`, isolated at `/tmp/slice6-20260830T194831Z-994c939b/target-static`, completed in 3m46s and is 102,449,024 bytes. `ldd` still lists `libstdc++.so.6` and `libgcc_s.so.1`; `objdump -T` still requires `GLIBCXX_3.4.29`, and local glibc symbols reach `GLIBC_2.39`. These flags did not capture Tier 2's benefit. There is no valid release-artifact size delta or older-host result. Sources: `raw/a3-local-static-build.log`, `a3-static-ldd.txt`, `a3-static-symbol-floors.txt`, `a3-static-size.txt`.

**A4: waived, environment.** The request log is absent, 12 views are not present, and no hot metadata was found. Counting only 7 files would be the inadmissible allowlist-only shortcut.

Group B is waived because Group A did not establish its gate. The reason is incomplete, non-admissible Group-A inputs, not an inference that removal survived.

## §11 table, as filled by this run.

| Property | DuckDB baseline | Best Rust candidate | Result |
| --- | ---: | ---: | --- |
| correctness | not measured | not measured | not measured |
| representative query p50/p99 | not measured | not measured | not measured |
| high-cardinality aggregate | not measured | not measured | not measured |
| concurrent throughput | not measured | not measured | not measured |
| peak RSS | not measured | not measured | not measured |
| ingest throughput | not measured | not measured | not measured |
| restart-to-ready | not measured | not measured | not measured |
| clean debug/release build | not measured | not measured | not measured |
| final binary | 102,449,024 bytes, local static-flag build only | not measured | not a valid delta |
| C++ compilation | libstdc++ and libgcc remain dynamically linked | not measured | A3 failed locally |
| external services | not measured | not measured | not measured |

## Corrections.

None to measured figures. The initial SSH-shell diagnosis was corrected before measurement: `~/.cargo/bin` was absent from `PATH`; the repository resolves pinned 1.95.0 once it is set.

## What I think.

There is no admissible decision from this run. A2 weakens the demonstrated-user case for C++ closure, and A3 says the proposed static flags do not remove its ABI tail. Neither answers whether DuckDB can be removed. The existing concurrency comparison is not carried forward because it is a synthetic fixture and its stated cause is deployment shape. The contrary pool test cannot run on this corpus because it lacks hot+cold state and the server's 32-client shape.

## Decision.

No admissible RFC-0042 decision: the required checkout, hot+cold corpus state, view population and request log are unavailable on this host.

## What would change my mind.

The most decision-changing measurement remains A1 on the specified hot+cold 12-view corpus: a 4/8/16-connection DuckDB pool reaching at least 0.7 times Rust qps with p99 below twice Rust at 32 clients would remove the concurrency regression from the ledger.
