[nest]
name = "usdc-120"
chain = "mainnet"
chain_id = 1
# Keyless. The tape is recorded with `--rpc`; a published number never carries a key.
rpc_urls = [
    "https://eth.drpc.org",
    "https://eth-pokt.nodies.app",
]
schema_version = 2
block_timestamps = true

[[contracts]]
alias = "usdc"
address = "0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48"
start_block = 6082465
abi = "abis/usdc.json"
events = ["Transfer"]
