{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nuthatch-indexer.com/eval-report.schema.json",
  "title": "nuthatch eval report (RFC-0016 §1, Tier B)",
  "description": "Every published agent-eval number traces to a document conforming to this schema. Hand-typed scores are forbidden; the report is generated by the keyed runner. CI validates the schema even when the run itself is skipped (no key present).",
  "type": "object",
  "required": ["date", "commit", "model", "temperature", "question_set_hash", "runs", "summary", "results"],
  "additionalProperties": false,
  "properties": {
    "date": { "type": "string", "description": "ISO-8601 date the run was produced." },
    "commit": { "type": "string", "description": "The nuthatch commit the surface was evaluated at." },
    "model": { "type": "string", "description": "The exact model id the agent ran (a score is a number about a pairing, not a universal constant)." },
    "temperature": { "type": "number" },
    "question_set_hash": { "type": "string", "description": "Hash of questions.toml, so a score is pinned to the exact questions." },
    "runs": { "type": "integer", "minimum": 1, "description": "Number of repeats the medians are taken over (>=3 for a published headline)." },
    "summary": {
      "type": "object",
      "required": ["first_try_pass_rate", "overall_pass_rate", "mean_sql_attempts"],
      "additionalProperties": false,
      "properties": {
        "first_try_pass_rate": { "type": "number", "minimum": 0, "maximum": 1 },
        "overall_pass_rate": { "type": "number", "minimum": 0, "maximum": 1 },
        "mean_sql_attempts": { "type": "number", "minimum": 0, "description": "Mean SQL attempts per question - the S3 (errors-as-prompts) success metric." },
        "by_class": {
          "type": "object",
          "description": "Optional per-class breakdown of overall pass rate.",
          "additionalProperties": { "type": "number", "minimum": 0, "maximum": 1 }
        }
      }
    },
    "results": {
      "type": "array",
      "description": "Per-question outcome (median run).",
      "items": {
        "type": "object",
        "_note": "final_query/final_rows are optional here and MANDATORY in the runner: the published 0.4 baseline predates them and genuinely lacks them, and backfilling null would assert the subject issued no SQL - a different claim from 'nobody recorded it'. validate_report refuses any new report that omits them (#1051).",
        "required": ["id", "class", "passed", "first_try", "sql_attempts", "tool_calls"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "class": { "type": "string" },
          "passed": { "type": "boolean" },
          "first_try": { "type": "boolean", "description": "Passed on the agent's first SQL attempt." },
          "sql_attempts": { "type": "integer", "minimum": 0 },
          "tool_calls": { "type": "integer", "minimum": 0 },
          "final_query": { "type": ["string", "null"], "description": "The last SQL the subject issued, or null if it issued none. Why a zero is a zero: without it a failing result cannot say whether the agent invented a table name, tripped the value/value_dec big-int footgun, or fell over the \"from\"/\"to\" reserved words. Unrecoverable after the run - the transcripts are gone (#1051)." },
          "final_rows": { "type": ["array", "null"], "items": { "type": "object" }, "description": "What that query returned, recorded on failures only (null on a pass, where the rows are the expected ones by definition). Distinguishes a wrong answer from an empty one." }
        }
      }
    }
  }
}
