# semantic.toml - what this nest's data *means* (RFC-0016). Read by the MCP `schema`
# tool, the admin UI, and the scaffolded skill. Edit descriptions freely; the
# `[table.*.footguns]` are DERIVED from the ABI and regenerated - leave them be.

schema_version = 1

[nest]
description = "The `obib-case3` nest on mainnet. (seeded from the ABI - edit semantic.toml to improve this)"

[table.blocks]
description = "`` events emitted by the `chain` contract. (seeded from the ABI - edit semantic.toml to improve this)"
grain = "one row per  event"

[table.blocks.columns]
base_fee_per_gas = "The `base_fee_per_gas` uint64 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
gas_limit = "The `gas_limit` uint64 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
gas_used = "The `gas_used` uint64 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
miner = "The `miner` address parameter. (seeded from the ABI - edit semantic.toml to improve this)"
parent_hash = "The `parent_hash` bytes32 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
size = "The `size` uint64 parameter. (seeded from the ABI - edit semantic.toml to improve this)"
transaction_count = "The `transaction_count` uint64 parameter. (seeded from the ABI - edit semantic.toml to improve this)"

[view]

# Authored views (views/*.sql) are described here so the MCP/`/schema` can render them:
# [view.your_view_name]
# description = "What this derivation computes."
